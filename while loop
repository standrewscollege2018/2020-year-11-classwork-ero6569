# set up list to save movies
movies = []

keep_asking = True:

while keep_asking == True:
    movie_name = input("Enter a favourite movie: ")
    if movie_name == "stop"
    keep_asking = False:
    else: 
        moies.append(movie_name)
        
print(movies)
#loop through list ad display the movies
for movie in movies:
print(movie)